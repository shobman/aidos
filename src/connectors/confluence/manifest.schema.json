{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AIDOS Publish Manifest",
  "description": "Configuration for publishing AIDOS artifacts to external targets",
  "type": "object",
  "required": ["publish"],
  "additionalProperties": false,
  "properties": {
    "publish": {
      "type": "object",
      "description": "Publish targets — each key is a target name with its own config",
      "properties": {
        "confluence": {
          "type": "object",
          "required": ["baseUrl", "rootPageId"],
          "additionalProperties": false,
          "properties": {
            "baseUrl": {
              "type": "string",
              "format": "uri",
              "description": "Confluence Cloud base URL, e.g. https://example.atlassian.net"
            },
            "rootPageId": {
              "type": "string",
              "description": "ID of the parent page — all artifacts become child pages under this"
            }
          }
        }
      },
      "additionalProperties": true
    }
  }
}
