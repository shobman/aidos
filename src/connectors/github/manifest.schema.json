{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AIDOS Manifest",
  "description": "Configuration for AIDOS artifact publishing. The github connector takes no manifest configuration from v1.2.0 onward — every save lands on the shared `aidos` branch, and a persistent rolling PR to the repo's default branch is maintained by the aidos.yml workflow.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "publish": {
      "type": "object",
      "description": "Publish targets (Confluence, etc.)",
      "additionalProperties": true
    }
  }
}
